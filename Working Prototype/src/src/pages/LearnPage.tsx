import { useState } from "react";
import { Cpu, HardDrive, MemoryStick, Battery, Fan, CircuitBoard, Usb, BookOpen } from "lucide-react";
import { lessons } from "../data/demoData";
const icons:any={CPU:Cpu,GPU:Cpu,RAM:MemoryStick,SSD:HardDrive,BOARD:CircuitBoard,BAT:Battery,FAN:Fan,PCIe:Cpu,USB:Usb};
export default function LearnPage(){const [group,setGroup]=useState("ALL");const [open,setOpen]=useState<string|null>(null);const filtered=group==="ALL"?lessons:lessons.filter(x=>x.group===group);return <div className="module-page"><section className="module-hero"><div><span className="eyebrow">MODULE 04 · KNOWLEDGE CORE</span><h1>LEARN MODE</h1><p>Understand laptop hardware through WHAT · WHY · HOW explanations.</p></div><div className="hero-chip"><BookOpen size={14}/> BEGINNER MODE</div></section><div className="filter-row">{["ALL","CORE COMPONENTS","UPGRADEABLE COMPONENTS","THERMAL SYSTEM","POWER SYSTEM","INTERFACES"].map(g=><button key={g} className={group===g?"active":""} onClick={()=>setGroup(g)}>{g}</button>)}</div><div className="lesson-grid">{filtered.map(l=>{const Icon=icons[l.icon]||Cpu;return <article className={`lesson-card ${open===l.id?"open":""}`} key={l.id} onClick={()=>setOpen(open===l.id?null:l.id)}><div className="lesson-top"><div className="lesson-icon"><Icon size={19}/></div><div><h3>{l.title}</h3><small>{l.group}</small></div><ChevronRight size={16}/></div><p>{l.what}</p><div className="lesson-tabs"><b>WHAT</b><b>WHY</b><b>HOW</b></div>{open===l.id&&<div className="lesson-expanded"><strong>WHY</strong><p>{l.why}</p><strong>HOW</strong><p>{l.how}</p></div>}</article>})}</div></div>}
